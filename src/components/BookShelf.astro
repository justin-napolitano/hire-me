---
import { bookShelfEntries } from '../config/profile';
---
<section class="rounded-3xl border border-rose-50/10 bg-[#160f0c]/80 p-8">
	<header class="mb-6 flex flex-col gap-2">
		<p class="text-xs uppercase tracking-[0.18em] text-rose-300/80">Nightstand lineup</p>
		<h2 class="text-3xl font-semibold text-white">Books I keep highlighted for real conversations</h2>
		<p class="text-base text-slate-300">
			Yes, I take notes in the margins and quote them at brunch. That's the point.
		</p>
	</header>
	<div class="grid gap-4 md:grid-cols-2">
		{bookShelfEntries.map((book) => (
			<article class="rounded-2xl border border-rose-50/10 bg-[#1d1410]/80 p-5 shadow-inner shadow-amber-400/10">
				<div class="flex items-center justify-between text-xs uppercase tracking-[0.2em] text-slate-400">
					<span>Annotated</span>
					{book.edition && <span class="text-right text-[0.65rem] text-slate-500">{book.edition}</span>}
				</div>
				<h3 class="mt-3 text-xl font-semibold text-white">{book.title}</h3>
				<p class="text-sm text-rose-300/80">{book.author}</p>
				<p class="mt-3 text-sm text-amber-200/90">{book.flex}</p>
				<p class="mt-2 text-sm text-rose-100/90">{book.highlight}</p>
			</article>
		))}
	</div>
</section>
